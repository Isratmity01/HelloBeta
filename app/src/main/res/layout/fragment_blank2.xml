<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@drawable/background"
    android:padding="@dimen/home_spacing"
    app:layoutManager="android.support.v7.widget.LinearLayoutMan‌​ager"
   >

    <!-- TODO: Update blank fragment layout -->
    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"


        android:id="@+id/numberInputLayout"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true">
        <ProgressBar
            android:layout_width="100dp"
            android:layout_gravity="center"
            android:id="@+id/contactsloader"
            android:layout_height="100dp" />

        <android.support.v7.widget.RecyclerView
            android:layout_below="@+id/frag"
            android:layout_width="match_parent"
            android:requiresFadingEdge="horizontal|vertical"
            android:layout_height="match_parent"
            android:id="@+id/contacts_list"></android.support.v7.widget.RecyclerView>


    </LinearLayout>

</FrameLayout>
